import React, {useState,useEffect} from 'react';

type ConfirmAlertProps = {}

const ConfirmAlert: React.FC<ConfirmAlertProps> = (props) => {
    const {} = props;
    return (
        <>
        </>
    );
};

export default ConfirmAlert;
